tests:

before = File.read("start.html")
result = HtmlToc.process(before, true, (3..5))


Testing result:

h1 should be unchanged
h2 should be unchanged
h3 should be changed
h4 should be changed
h5 should be changed
h6 should be unchanged

it should produce a table of contents with x rows
if a header starts with an id attribute, it should remain unchanged
if a header does not start with an id attribute, one should be added
if a header has an id added, it should be unique to the document

